extends layout

block content
  .container
    .row
      .col-md-10.col-md-offset-1
        h2 Import from resource file
        p.lead The repository comes with resource files of cards and categories that you can initially populate the database with. You can pre-load cards and categories from these server-local resource files if you want. To do so, use the buttons below. If there is already a card with the same ID in the database that card will not be loaded from file.
        
    .row
      .col-md-4.col-md-offset-1
        a.userlink(href='/cards/import') 
          button.btn-block(type="button" class="btn btn-success") Load cards resource file.

    .row
      .col-md-10.col-md-offset-1
        h2 Generate resource file
        p.lead You can also export the cards you have created to a resource file.

    .row
      .col-md-4.col-md-offset-1
        a.userlink(href='/cards/export')
          button.btn-block(type="button" class="btn btn-success") Export cards to resource file.

    .row
      .col-md-10.col-md-offset-1
        h2 Account creation tokens

    .row
      .col-md-4.col-md-offset-1
        a.userlink(href='/setup/usertoken')
          button.btn-block(type="button" class="btn btn-success") Create new create user token.
        p.lead List of current account tokens.


    if tokens.length > 0
      .row
        .col-md-4.col-md-offset-1
          each token in tokens
            .center-text
              h2
                span.label.label-primary #{token.token}
    else
      .row
        .col-md-4.col-md-offset-1
          p No tokens
