extends layout

block content
  .container
      .row.col-md-10.col-md-offset-1
          h2 User profile

          p #{usr}

          p #{user.email}
          p #{user.name}
          p #{user.id}